# ক্রিপ্টো সিগন্যাল জেনারেটর - ব্যবহারের নির্দেশিকা

এই প্রজেক্টটি ব্যবহার করে ক্রিপ্টো মার্কেট থেকে প্রতিদিন ১-৩টি হাই-কনফিডেন্স ইন্ট্রাডে সিগন্যাল পাওয়া যাবে। নিচে ধাপ-এ ধাপ-এ দেওয়া হলো কীভাবে এটি সেটআপ এবং রান করবেন।

## ১. প্রয়োজনীয় জিনিস (Prerequisites)
- **Node.js**: আপনার পিসিতে Node.js ইনস্টল থাকতে হবে।
- **MongoDB**: একটি লোকাল MongoDB ডাটাবেস রান থাকতে হবে (Default URL: `mongodb://127.0.0.1:27017`)।
- **Telegram Bot**: একটি বট ক্রিয়েট করে তার `Token` এবং আপনার `Chat ID` থাকতে হবে।
- **Gemini API Key**: Google AI থেকে একটি ফ্রি `GEMINI_API_KEY` নিয়ে আসতে হবে সিগন্যাল স্কোরিং-এর জন্য।

## ২. সেটআপ পদ্ধতি (Setup Procedure)
ধাপে ধাপে অনুসরণ করুন:

১. **Environment Variables**: 
প্রজেক্টের মূল ফোল্ডারে (Root directory) `.env` নামে একটি ফাইল ক্রিয়েট করুন (যদি না থাকে) এবং `.env.example` থেকে সব কন্টেন্ট কপি করে সেখানে বসান। নিচের ভ্যালুগুলো আপনার তথ্য দিয়ে পূরণ করুন:
```bash
GEMINI_API_KEY=আপনার_জেমিনি_এপিআই_কি
TELEGRAM_BOT_TOKEN=আপনার_টেলিগ্রাম_বট_টোকেন
TELEGRAM_CHAT_ID=আপনার_টেলিগ্রাম_চ্যাট_আইডি
BINANCE_API_URL=https://www.binance.com/api/v3/uiKlines
```

২. **Dependencies Install**:
টার্মিনাল ওপেন করে নিচের কমান্ডটি রান করুন:
```bash
npm install
```

## ৩. বট রান করার কমান্ড (Bot Run Commands)
প্রজেক্টটি রান করার জন্য টার্মিনালে নিচের কমান্ডগুলো ব্যবহার করুন:

- **প্রজেক্ট বিল্ড করার জন্য (প্রথমবার বা কোডে কোনো পরিবর্তন করলে এটি করতে হবে)**:
  ```bash
   npm run build
  ```

- **বট শুরু করার জন্য**:
  ```bash
  npm start
  ```

## ৪. বট কীভাবে কাজ করে?
- প্রতি ১ মিনিটে বট Binance থেকে BTC/USDT ডাটা সংগ্রহ (fetch) করবে।
- তিনটি টাইমফ্রেম (1H, 15m, 5m) অ্যানালাইসিস করে দেখবে সব শর্ত পূরণ হয়েছে কিনা।
- যদি ভালো সিগন্যাল পাওয়া যায়, তবে সেটি Gemini AI-এর কাছে পাঠানো হবে যাচাই করার জন্য।
- যদি AI স্কোর **৭০-এর বেশি** হয়, তবেই এটি টেলিগ্রামে সিগন্যাল হিসেবে পাঠানো হবে।
- প্রতিটি সিগন্যাল MongoDB-তে সেভ করা হবে যাতে পরবর্তীতে চেক করা যায়।
- প্রতিদিনের শেষে একটি সারাংশ (Summary) রিপোর্ট টেলিগ্রামে পাওয়া যাবে।

**সতর্কতা**: ট্রেডিং ঝুঁকির্পূর্ণ, তাই সব সময় স্টপ লস ব্যবহার করবেন এবং নিজের বুঝমতো ট্রেড করবেন। শুভকামনা! 🚀
